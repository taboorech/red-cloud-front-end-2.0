// Shared Tailwind class constants for theme-aware UI primitives.
// Page-level uppercase tracking labels (the "PLAYLIST", "PROFILE" eyebrows above page titles).
export const PAGE_LABEL_CLASS =
  "text-[11px] tracking-[0.18em] font-semibold text-app-text-soft uppercase";

// Smaller field-level uppercase labels (e.g. above form inputs and cards).
export const FIELD_LABEL_CLASS =
  "text-[11px] tracking-[0.16em] font-semibold text-app-text-muted uppercase";

// Brand pill button (theme-aware). Use for primary CTAs on pages.
export const BRAND_BUTTON_BASE =
  "rounded-full bg-brand-500 hover:bg-brand-600 text-white font-semibold transition cursor-pointer disabled:opacity-60";
